{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Jekyll Server",
            "type": "shell",
            "command": "bundle exec jekyll serve",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}